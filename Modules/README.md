This folder contains PowerShell modules required at runtime by Azure Functions
when running on Linux or Flex Consumption plans where Managed Dependencies
are not supported.

Only /Modules/Required is committed.
All other module folders are ignored.